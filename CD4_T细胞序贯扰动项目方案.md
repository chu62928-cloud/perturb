# CD4 T细胞序贯扰动项目方案

**版本：2026-09-01**

## 一、项目一句话定义

利用大规模原代人CD4 T细胞全基因组扰动数据，调用多个现有扰动预测模型作为“执行器”，构建一个**可信、可组合、考虑异质性和模型不确定性的序贯规划器**，寻找能将给定T细胞状态推向目标状态的最优短序列CRISPR干扰方案，并最终在Th2→Th17方向进行真实顺序实验验证。

核心问题不是“能不能预测一次扰动”，而是：

> **单步扰动预测能否被可靠地连续调用，并在模型不完全可信时仍给出可实验验证的最优扰动顺序？**

这应当是项目的方法学主线，也是与（DiRL）最需要拉开的差异。

---

## 二、项目定位与创新点

### 1. 不再开发新的单步扰动预测器

现有扰动预测模型已经很多，而且更新很快。项目不应把主要精力投入“再造一个执行器”，而是建立统一接口调用多个执行器，并比较、集成它们。

### 2. 主要方法学创新放在规划器

建议集中在四点：

1. **可组合性评价**：单步预测准确不等于连续两步后仍然准确，建立专门的两步可组合性测试。
2. **分布式状态转移**：一个动作输出一群可能的细胞，而不是一个平均细胞或最近邻细胞。
3. **不确定性感知规划**：同时考虑模型分歧、训练分布支持度和校准后的预测区间，避免规划器利用模型漏洞。
4. **保守短程搜索**：先做两步，验证通过后再扩展到三步；不一开始追求长链强化学习。

### 3. 生物学最终亮点

真正需要争取的结果不是“两个基因都重要”，而是：

- A→B优于B→A；
- A→B优于A+B同时扰动；
- 并且该顺序促进Th2向稳定Th17样状态转换。

如果成立，说明**扰动顺序本身具有不可交换的生物学效应**。

---

## 三、数据与任务层级

### 主训练数据

采用Marson/Pritchard团队原代人CD4 T细胞全基因组CRISPR干扰数据：约2200万细胞、4个供体、静息/刺激8小时/刺激48小时三个条件。该数据特别适合学习状态依赖和刺激背景依赖的单基因扰动效应。

注意：三个刺激条件是不同细胞群体的终点，不是同一细胞的纵向轨迹，因此不能直接解释为真实三步时序。

### 验证任务

优先选择已有知识和外部数据较丰富的方向：

1. Th0→Th1；
2. 初始/Th0样→Th2；
3. Th1→Th2。

这些任务用于证明执行器与规划器是否可信。

### 最终发现任务

**Th2→Th17**。

建议使用至少两个独立的人源Th17单细胞数据集定义目标状态，建立“共识Th17状态”，避免模型只逼近某一个数据集的批次特征。

---

## 四、执行器选择：推荐三个核心模型

### 1. （STATE）：第一核心执行器

**为什么选：**

- 专门面向跨细胞背景的扰动预测；
- 直接对一组细胞建模，同时保留单细胞分辨率；
- 很适合“异质细胞群体 + 无法一一配对”的Perturb-seq；
- 官方代码已经支持遗传扰动训练和推断；
- 与本项目“当前细胞群体 + 基因敲低 → 下一状态分布”的定义最匹配。

**在项目中的角色：**主执行器，重点负责状态依赖的群体转移预测。

### 2. （PRiMeFlow）：第二核心执行器

**为什么选：**

- 2026年的分布流匹配模型，直接在基因表达空间生成扰动后细胞分布；
- 强项正是细胞异质性和分布拟合；
- 在Arc虚拟细胞挑战中获得最稳健的综合表现；
- 公开训练流程已经整合大量扰动数据，仓库中特别提到12个CD4 T细胞数据文件；
- 与（STATE）架构差异较大，适合做模型集成和模型分歧估计。

**在项目中的角色：**主执行器，重点负责完整表达分布和异质性生成。

### 3. （STACK）：第三执行器/泛化检查器

**为什么选：**

- 2026年发布，主打上下文学习和跨细胞背景零样本泛化；
- 不要求在目标细胞状态中重新获得大量扰动训练数据；
- 特别适合本项目后期从Marson数据外推到外部Th2/Th17状态；
- 与前两个监督扰动模型形成结构上的独立性，因此很适合判断某条路径是否只是单一模型的偏差。

**在项目中的角色：**跨背景泛化与独立复核，不要求它单独决定最终路径。

### 必须保留的基线

- 简单线性/残差模型；
- （GEARS）作为经典遗传扰动预测基线。

原因：近年的系统评测已经表明，复杂模型并不总能稳定超过简单线性基线。如果核心模型在严格留出测试中没有明显优势，就不应直接进入多步规划。

### 暂不作为核心执行器

- （scGPT）：可作为补充基线，但（DiRL）已经大量依赖它，而且最新评测中并非稳定最优。
- （CPA）：适合组合与协变量建模，但对完全未见遗传扰动和复杂状态分布的优势不如上述三者明确。
- （PerturbNet）：很适合未见基因和分布预测，可作为第四候选，但本项目更关心“已见基因在未见细胞状态中的效应”，因此优先级低于（STACK）。
- （scDFM）：2026年很值得关注，但与（PRiMeFlow）同属分布流匹配路线，第一阶段不必同时投入两个高度相似的执行器。
- （MultiFlow）：适合未来RNA+ATAC联合扰动预测；当前主Perturb-seq没有匹配ATAC，不建议第一阶段加入。

---

## 五、统一执行器接口

无论使用哪个模型，都强制转成相同输入输出：

**输入：**

- 当前细胞群体或局部细胞状态分布；
- 基因CRISPR干扰动作；
- 刺激条件/供体等必要协变量。

**输出：**

- 扰动后细胞状态分布；
- 目标状态改善量；
- 模型内部或重采样不确定性；
- 与训练数据流形的距离。

执行器不负责“选哪个基因”，只负责回答：

> 如果当前是这些细胞，现在敲低基因A，下一状态最可能是什么？

---

## 六、规划器：项目真正的方法主体

### 1. 状态不是单个平均向量

用一组细胞或其潜空间分布表示当前状态。

避免（DiRL）式“预测后强制投到单个最近真实细胞”。建议采用**软流形约束**：允许预测新状态，但如果远离真实训练分布就增加惩罚。

### 2. 三种不确定性同时进入规划

对每个“状态-动作”组合计算：

1. **模型分歧**：三个执行器预测是否一致；
2. **分布外程度**：当前状态和预测状态是否有真实数据支持；
3. **校准误差区间**：利用完全留出的真实Perturb-seq做保形校准。

最终不优化平均预测值，而优先优化“较保守的可信下界”。

### 3. 奖励函数

对于Th2→Th17，建议至少包含：

- 提高Th17身份程序；
- 降低Th2身份程序；
- 靠近共识Th17细胞分布；
- 惩罚应激、凋亡和纯粹的广泛激活；
- 惩罚模型分歧；
- 惩罚分布外状态。

不要只用欧氏距离或单一分类概率。

### 4. 搜索策略

**第一阶段：两步束搜索。**

先对所有候选第一步扰动评分，保留前K个，再展开第二步。两步空间经过候选预筛选后完全可控，不需要强化学习。

**第二阶段：三步鲁棒蒙特卡洛树搜索。**

只有在两步可组合性得到验证后，再扩展到三步。树节点保存的是状态分布和不确定性，而不是单个细胞。

**实际实验阶段：滚动重规划。**

完成第一步并获得真实数据后，重新输入真实新状态，再规划第二步，而不是一次性相信完整长路径。这相当于模型预测控制思想。

---

## 七、最重要的新基准：扰动“可组合性”

这是建议重点发展的方法学贡献。

### 问题

一个模型可能在单步测试中很好：

S + A → S_A

但真正规划需要：

S → A → S_A → B → S_AB

第二步输入已经是模型生成或扰动改变后的状态，误差可能迅速累积。

### 代理两步测试

在真实数据中构造：

1. 从状态S预测A后的状态Ŝ；
2. 在真实未扰动细胞中寻找与Ŝ接近的局部状态N；
3. 数据中保留N状态下基因B的真实扰动细胞作为隐藏真值；
4. 比较模型从Ŝ继续预测B的结果与真实N+B结果。

重点评价：

- 第二步目标程序方向是否正确；
- 细胞分布距离；
- 差异基因恢复；
- 不确定性是否能识别错误预测；
- 两个动作交换顺序后预测是否稳定且有意义。

这比普通“未见基因预测”更接近最终序贯规划的真实使用场景。

---

## 八、具体执行顺序

### 阶段0：冻结问题定义（1周）

- 明确第一版只做CRISPR干扰，不做过表达；
- 最大规划深度先定为2；
- 确定3个验证方向和Th2→Th17最终方向；
- 确定两个独立Th17外部参考数据。

**输出：**固定的数据划分、评价指标、目标状态定义。

### 阶段1：数据与目标状态空间（2-3周）

- 整理Marson细胞级数据和质量控制字段；
- 建立跨供体、跨刺激条件的共享细胞状态空间；
- 外部Th1/Th2/Th17数据映射到共享空间；
- 定义身份程序、应激程序和目标分布。

**输出：**统一状态表示和目标评分函数。

### 阶段2：执行器适配与严格测试（4-8周）

依次完成：

1. 线性基线；
2. （STATE）；
3. （PRiMeFlow）；
4. （STACK）；
5. （GEARS）基线。

测试必须包含：

- 留出整个基因；
- 留出供体；
- 留出刺激背景；
- 最重要：留出“基因×局部细胞状态”组合。

**闸门：**如果复杂模型不能在与规划相关的留出任务上稳定优于线性基线，先解决执行器问题，不进入多步规划。

### 阶段3：可组合性与不确定性校准（3-5周）

- 建立代理两步测试；
- 测量每个执行器两步误差增长；
- 建立模型集成；
- 用真实留出数据做保形校准；
- 建立分布外检测。

**输出：**每个状态-动作的可信度评分。

### 阶段4：两步规划器（2-4周）

- 候选基因预筛选；
- 两步束搜索；
- 比较单模型、模型集成、无不确定性、保守规划四种版本；
- 与（DiRL）思路和简单贪心策略比较。

**输出：**稳定的两步候选序列及其置信度。

### 阶段5：已知分化方向回顾性验证（2-3周）

优先：

- Th0→Th1；
- 初始/Th0样→Th2；
- Th1→Th2。

要求候选调控因子和方向与已知生物学、Marson论文中的极化结果或独立数据基本一致。

**闸门：**如果已知方向无法恢复，不直接进入Th2→Th17实验。

### 阶段6：Th2→Th17发现（1-2周）

输出：

- 最佳单敲；
- 最佳同时双敲候选；
- 最佳A→B；
- 最佳B→A；
- 三个执行器的预测一致性；
- 两个独立Th17目标数据集的一致性。

优先选择5-10对最稳健的序列进入实验。

### 阶段7：真实顺序实验验证

每对至少比较：

- 对照；
- A单敲；
- B单敲；
- A+B同时；
- A→B；
- B→A。

如果A→B显著优于B→A和同时双敲，才可以强力声称“顺序效应”。

实验完成第一步后，应尽可能测量真实中间状态，并据此做一次滚动重规划。

---

## 九、项目最重要的成功判据

1. 至少两个核心执行器在“基因×细胞状态”留出任务上表现稳定，并优于简单基线；
2. 不确定性评分能识别明显错误的第二步预测；
3. 两步保守规划优于单步贪心和不考虑不确定性的规划；
4. 已知T细胞极化方向能够被合理恢复；
5. Th2→Th17的关键候选在至少两个执行器、两个目标数据集和多个供体上方向一致；
6. 湿实验出现真实的A→B与B→A差异。

---

## 十、建议论文主线

### 方法学主结论

> 单步扰动预测模型不能被默认视为可无限连续调用的细胞模拟器；通过真实Perturb-seq校准、可组合性评价、模型集成和不确定性感知规划，可以得到更可靠的短序列细胞状态控制策略。

### 生物学主结论

> 某些T细胞状态转换不仅依赖扰动组合，还依赖扰动顺序；该顺序能够从大规模单基因Perturb-seq中被计算推断并经实验验证。

### 与（DiRL）的差异

不要主张“首次用多步决策控制细胞”。建议突出：

- （DiRL）主要把虚拟扰动模型当作环境并学习策略；
- 本项目把“环境是否可信、是否可组合”本身作为核心问题；
- 使用真实原代T细胞全基因组Perturb-seq校准多个执行器；
- 显式处理模型分歧、分布外和统计校准；
- 最终做A→B、B→A、同时扰动的真实顺序实验。

---

## 十一、最新模型搜索后的选择结论

截至2026年9月，扰动预测领域已经进入“分布建模、跨背景泛化、流匹配、上下文学习”阶段。最新值得关注的模型包括（STATE）、（STACK）、（PRiMeFlow）、（scDFM）、（PerturbNet）、（PRESAGE）以及RNA+ATAC方向的（MultiFlow）。同时，2025-2026年的大型评测和Arc虚拟细胞挑战都提示：复杂模型并非自动优于简单基线，模型选择必须依赖与实际使用场景一致的严格留出测试。

因此，本项目不押注单个所谓“最强模型”，而采用**结构差异明显的三个执行器 + 简单基线 + 统一规划器**。这是更稳健、也更容易形成方法学贡献的路线。

---

## 十二、主要参考资料

1. Zhu等，2026，《Cell》：原代人CD4 T细胞全基因组Perturb-seq。  
   https://www.sciencedirect.com/science/article/pii/S0092867426009293

2. （STATE），2025：跨细胞背景扰动预测。  
   https://www.biorxiv.org/content/10.1101/2025.06.26.661135v2  
   https://github.com/ArcInstitute/state

3. （STACK），2026：基于上下文学习的跨背景单细胞预测。  
   https://www.biorxiv.org/content/10.64898/2026.01.09.698608v2  
   https://github.com/ArcInstitute/stack

4. （PRiMeFlow），2026：基于流匹配的分布式扰动响应模型。  
   https://github.com/altoslabs/primeflow

5. （scDFM），2026，（ICLR）：分布流匹配扰动预测。  
   https://proceedings.iclr.cc/paper_files/paper/2026/hash/75f029a1289a47aa99c86588239f0c12-Abstract-Conference.html

6. （PerturbNet），2025：预测未见遗传/化学扰动后的细胞状态分布。  
   https://doi.org/10.1038/s44320-025-00131-3

7. 2026《Nature Methods》系统评测：27种方法、29个数据集，强调跨细胞背景泛化仍是主要困难。  
   https://www.nature.com/articles/s41592-025-02980-0

8. 2025《Nature Methods》：深度扰动预测模型并不稳定优于简单线性基线。  
   https://www.nature.com/articles/s41592-025-02772-6

9. Arc虚拟细胞挑战2025总结：混合统计与深度方法、分布式生成模型表现突出。  
   https://arcinstitute.org/news/virtual-cell-challenge-2025-wrap-up

10. 鲁棒蒙特卡洛规划，2025（ICML）：处理模拟器和真实动力学不一致。  
    https://proceedings.mlr.press/v267/dam25a.html

11. 分布外自适应模型预测控制，2025：模型集成、分布外监测与保形校准。  
    https://proceedings.mlr.press/v283/contreras25a.html

12. 分布漂移下的鲁棒保形规划，2026：对生成模型误差和环境变化进行显式校准。  
    https://proceedings.mlr.press/v331/rahaman26a.html

13. （MultiFlow），2026：RNA与ATAC联合扰动预测，可作为项目第二阶段多组学扩展。  
    https://www.biorxiv.org/content/10.64898/2026.08.20.746112v1

---

## 十三、实施附录：数据与模型环境状态（2026-09-01）

### 1. 数据存储闸门

- 预期的12个原始 `h5ad` 文件中，目前服务器已有7个；7个文件均通过文件大小、HDF5/AnnData只读打开、`obs/var` 唯一性以及首/中/尾切片读取检查。
- `D2_Stim8hr` 的截断问题已恢复，当前文件大小与源端记录一致。
- 尚缺 `D3_Rest`、`D3_Stim48hr`、`D4_Rest`、`D4_Stim48hr`、`D4_Stim8hr` 五个文件，合计约641.13 GiB；数据盘当前约260 GiB可用，因此暂不继续全量下载。继续下载的门槛为可用空间约800 GiB或挂载外部存储，且不删除现有数据。

### 2. 环境与模型边界

运行资料统一位于服务器 `/root/autodl-tmp/CRISPR_perturb_runtime/`，按 `repos/`、`models/`、`env_manifests/`、`smoke/` 和 `data_audit/` 分类保存。服务器显卡为 RTX 4080 SUPER（约32 GiB显存，驱动580.142）。

- `e3_state`：固定 STATE 提交 `9bbfe78a434a55205e4de834e1ea99f85f7a3add`；官方 `ST-HVG-Replogle` 检查点已下载并在 GPU 完成加载和最小前向。
- `e3_stack`：固定 STACK 提交 `cacc2e4b09435c3e536d46237d10b50f222dd144`；官方 `Stack-Large-Aligned` 检查点已下载并以微型细胞批次完成 GPU 前向。当前采用服务器上已验证的 PyTorch 2.11.0（CUDA 12.8）组合，而不是强制回退到仓库示例的 CUDA 12.1版本。
- `e3_primeflow`：固定 PRiMeFlow 提交 `443295258e364e394b60de27a861d8d1cf3b1fa6` 和 PerturBench 提交 `c84038bc1ea409aa54f3832cfa6f34f5059adf0c`；官方未公开可下载的预训练权重，本轮只保存并回载缩小 `DynamicsMLP` 检查点，不将其称为官方模型。
- `e3_gears`：固定 GEARS 提交 `f374e43e197b295016d80395d7a54ddb81cc6769`，使用 `cell-gears==0.1.2` 与 `torch-geometric==2.6.1`；完成小型模型的单基因扰动预测、检查点保存和回载。
- `e3-th-actuator`：保留数据分析和 `pert2state-model==0.0.1`，完成小矩阵拟合及预测；通过锁定 `boto3/botocore==1.42.90` 消除了原有的 `aiobotocore` 依赖冲突，不混入深度模型依赖。

每个环境均保存 `pip freeze`、Conda显式规格、Conda环境导出和 `pip check` 记录；当前五个环境的依赖检查均无未解决项。STATE、STACK、PRiMeFlow和GEARS的最小计算日志记录了输入/输出维度、耗时和峰值显存，模型加载成功不等同于已经完成项目适用性验证。

### 3. 网络与许可边界

GitHub源码和Hugging Face文件均在临时子进程中执行 `source /etc/network_turbo` 下载，未将代理写入 `.bashrc`、Conda环境或项目配置；PyPI/Conda仍使用独立的软件包源。STATE和STACK权重仅按其非商业学术许可使用，许可文件与模型文件一并保存，项目记录中不保存任何令牌。

### 4. 下一步执行顺序

1. 在完整的D1/D2三个条件上抽取50–100个靶基因，D1用于开发，D2锁定为外部供者测试。
2. 先冻结基因顺序、归一化尺度、非靶向对照和“基因×局部状态”划分，运行无变化、均值、线性残差和 `pert2state` 基线。
3. 只有在严格留出任务上小型STATE显著优于简单基线后，才开始PRiMeFlow和STACK的数据适配；至少两个执行器通过单步基准后，再进行代理两步测试、不确定性校准和两步束搜索。
4. GEARS的组合预测必须保留其限制：仅凭单基因训练不能假定组合扰动可靠，正式组合结论需要组合训练样本和真实实验验证。

## 十四、流水线实施状态（2026-09-01）

已在服务器 `/root/autodl-tmp/CRISPR_perturb_runtime/pipeline/` 部署独立的 `e3_pipeline` 编排环境（Python 3.11.16、Snakemake 8.30.0、AnnData 0.12.19、Scanpy 1.11.5）。流水线源码位于 `src/cd4perturb/`，包含：

- 数据注册、文件指纹与 D1/D2/D3/D4 角色管理；
- D1-only 的分块读取、96 基因结构试点和公共元数据审计；
- 无变化、条件均值、线性残差基线接口；
- 基因、局部状态、基因×状态三类留出接口；
- 分布级指标、生物学模型闸门、中间态支持度和代理组合性闸门；
- 模型分歧不确定性、可解释分项评分和深度2束搜索；
- 不可覆盖的 D2 锁定发布清单接口。

Snakemake 已实际完成 `preflight → audit → prepare_pilot → freeze_data`。当前7个完整文件均已登记；D1生成4,661个候选和96个结构试点（64个核心、32个挑战）。由于guide库校订、效应摘要、基因顺序/归一化哈希和功能程序尚未冻结，当前数据契约仍为 `DATA_CONTRACT_READY`，不能宣称 `DATA_VALID`，`MODEL_GLOBAL_VALID`、`MODEL_STATE_VALID`、`COMPOSITION_VALID` 和 `PLANNER_VALID` 均保持待定。验收记录见 `research/pipeline_acceptance_report.md`，运行锁定见 `research/runtime_manifests/e3_pipeline.*`。
## 十五、修订后的科学闸门与数据契约（2026-09-01）

本项目固定采用 `DATA_VALID → MODEL_GLOBAL_VALID → MODEL_STATE_VALID → COMPOSITION_VALID → PLANNER_VALID → EXPERIMENT_VALID` 的证据链，不允许用下一级结果替代上一级证据。

- 96基因试点拆为 `primary_64` 和 `challenge_32`。前者只能依据扰动前表达、guide覆盖和功能类别选择，作为主要统计评价集；后者允许依据D1效应强弱、刺激依赖和guide一致性选择，只作困难场景压力测试，不能进入主要模型闸门。
- 状态单位改为连续潜空间局部近邻。D1非靶向对照冻结50维空间，每个条件选择10个高密度锚点，以第200近邻距离定义区域，按五折每折留出两个锚点区域，并以1.25倍区域半径设置训练缓冲。Leiden仅用于描述、分层和可视化。
- D1置信区间采用 `gene→guide→cell` 或 `gene→region→guide→cell` 层级自助法，只表示技术和状态层级不确定性；供者层级泛化须待D2–D4完整后估计。
- 全局闸门与状态闸门分开评价：只有平均单步性能和基因×连续状态联合留出均通过，执行器才可进入组合性测试。
- 组合性严格分成几何匹配和结果评分两个阶段。匹配清单先按固定距离、密度和第二动作细胞数阈值生成并封存，之后才允许读取第二步真实响应；不足50个有效三元组、20个第二动作基因或3个状态区域时不得放行。
- Th2→Th17采用多目标安全契约：Th17上升、Th2下降，应激、凋亡和泛活化不超过D1非靶向对照第95百分位；排序分数为 `1.0×Th17 − 1.0×Th2 − 0.5×stress − 0.75×apoptosis − 0.5×general_activation`，安全约束不能被总分抵消。

当前已生成 `pilot_64_primary_v1.json`、`pilot_32_challenge_v1.json`、`pilot_manifest_v1.json` 和 `data_contract_frozen_v1.json`。前10步仍只使用D1，不读取D2响应；单步效应矩阵、程序基因集、实际潜空间和模型科学性能均待后续阶段完成。
