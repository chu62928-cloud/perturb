# 从原代 CD4 扰动数据到 Th2→Th17 少基因干预设计

版本：实施方案 v1.0｜调研与数据快照：2026-08-31｜性质：研究计划，不是已完成的模型或实验结果

本方案依据 `perturb_first_plan.md`、本次服务器只读核查、原始论文与作者代码整理。原始文件保持不变。文中的细胞数和文件状态来自实测；模型能力来自引用来源；训练规模、验收门槛和时间安排属于建议默认值，尚未通过本项目运行验证。

## 1. 我的判断与推荐

这个方向可以开展，但应把第一阶段定位为**面向 Th17 目标的、可审计的基因敲低候选提名**，不要立即承诺“从公共数据学习任意 Th2→Th17 多步命运控制”。目前最重要的缺口不是搜索算法，而是公共 CD4 扰动效应能否迁移到真实 Th2 起点，以及组合效果能否被独立实验验证。

建议保留原想法中的三个重点：以目标状态驱动选择、优先减少干预数量、考虑群体异质性。需要调整的是证据顺序：先验证目标与单基因效应，再验证背景迁移，随后研究组合；真实顺序控制单独设立升级条件。

创新性不能仅写成“扰动预测变为逆向设计”。同一 CD4 研究的 `pert2state_model` 已做目标特征重建，PDGrapher 已直接研究逆向组合靶点预测，稀疏选择与细胞命运转换也有先例。[R1](https://doi.org/10.1016/j.cell.2026.08.002)、[R3](https://github.com/emdann/pert2state_model)、[R4](https://doi.org/10.1038/s41551-025-01481-x)、[R17](https://doi.org/10.1093/bib/bbab311)

更值得争取的贡献是：**在真实 Th2 起点和明确实验约束下，确定何时能可靠地推荐少基因干预、何时应拒绝外推，并用独立供者和组合实验检验其效果及最小性。** 这仍是待证实的研究主张，不能据本次有限范围的调研声称全球首创。

| 阶段 | 核心产物 | 成立所需的新增证据 | 目前判断 |
|---|---|---|---|
| V0：可执行首版 | 单基因候选清单、证据等级、少量加性组合假设 | 公共参考样本审计、目标定义、质量过滤 | 可以立即开始准备与计算 |
| V1：单步响应与迁移 | 经独立测试的候选预测器 | 完整新供者；另外需要真实 Th2 的单基因干预 | 有条件可行，不能只靠 D1 自证 |
| V2：组合与实验闭环 | 经验证的少基因方案、效果—数量权衡 | 同背景单/双扰动及功能和持续性读出 | 推荐作为本课题最终主版本 |
| V3：真实时序控制 | 带时间、历史与持续效应的顺序策略 | A→B、B→A、同时干预及中间状态数据 | 可选远期扩展，不作为首篇工作的必达任务 |

如果 V0 的简单方法已能给出可靠候选，仍然应推进生物学验证；不必为了使用大型模型而改写问题。如果 V1 复杂模型没有优于简单基线，就保留简单模型，把研究资源用于验证和解释。

## 2. 当前实际拥有的数据与资源

### 2.1 服务器文件状态

路径：`/root/autodl-tmp/CRISPR_perturb`。截至本次 19:50 左右的只读审计：

| 文件 | 细胞数 × 特征数 | 实际字节数 | 状态 |
|---|---:|---:|---|
| `D1_Rest.assigned_guide.h5ad` | 3,074,496 × 18,130 | 142,828,875,662 | 可打开、元数据及首/中/尾行可读，源端大小一致 |
| `D1_Stim8hr.assigned_guide.h5ad` | 2,789,727 × 18,130 | 154,774,003,188 | 可读；此前源端大小与首尾片段哈希一致 |
| `D1_Stim48hr.assigned_guide.h5ad` | 2,805,354 × 18,130 | 156,246,167,022 | 可读；此前源端大小与首尾片段哈希一致 |
| `D2_Stim8hr.assigned_guide.h5ad` | 暂不采用 | 112,828,416,000 | **截断**；源端及 HDF5 内部预期均为 172,796,432,870 字节 |

以上不等于已对全部字节做源端校验。当前三个 D1 文件足以开展本背景分析，但 D2 不能用于训练或测试；待其完整恢复并通过核查后再纳入。这次没有续传或删除任何服务器数据。

三个 D1 文件的表达特征集合相同，但 48 小时文件的列顺序不同，必须按基因编号重排。当前 X 为 CSR 稀疏矩阵、`float32` 存储；作者定义其内容为 UMI 计数，本次抽样行也与此一致。没有现成的降维、邻接图或细胞类型注释可直接用于 Th2/Th17 任务。[R2](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/data_sharing_readme.md)

| 初步过滤后数量 | Rest | Stim8hr | Stim48hr |
|---|---:|---:|---:|
| 单 sgRNA 非靶向对照（NTC） | 76,543 | 69,632 | 73,534 |
| 单 sgRNA 靶向、原始靶基因编号非空 | 1,675,494 | 1,537,130 | 1,574,301 |

这里只排除了原文件标记的低质量细胞并按单 sgRNA/对照类型筛选，**尚未完成靶基因校订、敲低有效性与脱靶过滤**，不能把这些数当作最终训练规模。

### 2.2 三个必须修正的理解

1. D1 三个条件仍是一个供者。测序泳道、多个 sgRNA 和百万细胞不能替代独立供者。
2. Rest、Stim8hr、Stim48hr 是实验条件，不是 Th2、Th17 标签，也不是同一细胞的追踪轨迹。作者样本表还显示 D1 的 Rest/8h 在 R1、48h 在 R2，因此时间条件与处理批次部分混杂，单靠批次校正不能证明已分离二者。[R18](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/suppl_tables/sample_metadata.suppl_table.csv)
3. 一般 CD4 扰动数据可以帮助提出 Th2 假设，但不能替代已经极化的 Th2 在相同培养背景中的扰动证据。

### 2.3 资源快照

GPU 驱动报告约 32 GiB 可见显存；容器内存上限是 **62 GiB**，不是 `free` 显示的宿主机约 503 GiB。数据盘早先接近满，复查已恢复约 **396 GiB** 可用空间。资源可能随租用配置和下载变化，正式运行前应重查。

当前 `sc_fate_env` 的 Python 为 3.10.20，适合继续进行现有读取和预处理。当前 STATE 源码要求 Python ≥3.11 且 <3.13，CellFlow 要求 ≥3.11；后续分别建立独立环境，**不直接升级或覆盖现有环境**。[R15](https://github.com/ArcInstitute/state/blob/9bbfe78a434a55205e4de834e1ea99f85f7a3add/pyproject.toml)、[R16](https://github.com/theislab/CellFlow/blob/446ed6073c60ac2e8db13c4ea096a43cdec288b2/pyproject.toml)

## 3. 原方案哪些地方需要优化

| 原思路 | 问题 | 本方案的修改 |
|---|---|---|
| Perturb-seq 提供“状态—动作—新状态”训练对 | 通常是未配对的对照与扰动终点群体 | 学群体响应，明确匹配条件；不构造虚假的同细胞配对 |
| 用固定扰动向量做第一版 | 简单但适合作为基线 | 保留，分条件估计并报告迁移局限 |
| 学好单步模型即可递归做多步 | 第二步起既有分布偏移，也缺历史干预信息 | V1只做单步；组合用集合模型；时序另需实验 |
| `multi sgRNA` 可以训练组合 | 当前文件没有可靠的完整多靶点身份 | V0/V1排除，不把模糊标签当真实组合 |
| 非负弹性网络解决只敲低约束 | 非负不等于剂量；旋转扰动轴后约束还可能失效 | 在原始动作基上约束，系数仅作提名依据 |
| STATE 与 Stack 一致就更可靠 | 两模型可能共享偏差、预训练数据或提示 | 仅做分歧诊断；校准必须依赖独立目标真值 |
| 高表达相关性证明模型好 | 背景表达和平均扰动效应可能占主导 | 强制均值/线性基线、扰动特异指标与候选排名 |
| 转录组像 Th17 就是转换成功 | 可能是混合状态、应激或选择性存活 | 分开评价相似、功能获得与来源明确的持续转换 |
| 从第一阶段追求最优顺序 | 没有时序干预监督，搜索会利用模型误差 | 先优化已定义动作集合内的候选，再逐级补证 |

未配对数据并非没有因果价值：在同供者、同处理背景中，若实验设计支持扰动与匹配 NTC 的可交换性及共同支持，可以估计群体平均效应或终点分布差异。但这不唯一确定每个细胞的反事实、真实转化路径或干预先后关系。

一个直观的可识别性反例是，两个模型可以写为：

$$
\begin{aligned}
f_1(x,a,b,c)&=f_0(x,c)+a\Delta_A(x,c)+b\Delta_B(x,c),\\
f_2(x,a,b,c)&=f_1(x,a,b,c)+abH(x,c).
\end{aligned}
\tag{1}
$$

这里 a、b 为两个基因是否被干预的二元指示，c 是实验背景。对照和所有单基因数据中，乘积 ab 都为零；任意相互作用 H 都无法由这些单扰动数据区分。增加同类单扰动细胞数能提高精度，但不会凭空识别组合项。这是本方案的数学分析，不是某个模型已获验证的能力。

## 4. 现有方法的真实位置与创新空间

| 方法 | 已有证据支持的作用 | 在本项目的位置 | 不应扩大为 |
|---|---|---|---|
| pert2state | 用扰动效应重建目标签名、提名调控因子 | V0最直接基线 | 已验证真实Th2转换或最小组合 |
| STATE | 基于对照细胞集合、背景与干预预测终点响应 | V1优先小模型候选 | 无需适配的Th2模拟器、真实顺序控制器 |
| CellFlow | 条件分布建模；有组合及带时间培养方案的研究证据 | V1竞争模型，V2后考虑组合接口 | 流积分时间就是生物时间；序贯CRISPRi已解决 |
| Stack | 利用表达响应提示做跨背景生成 | 有合适提示时的附加基线 | 仅输入基因名便能预测；无真值校准 |
| GEARS | 单/双基因正向预测，依赖训练背景与组合信息 | V2组合对照 | 单扰动即可可靠学任意组合 |
| PDGrapher | 给定初始/期望状态，逆向预测组合靶点 | 必须讨论的近邻；数据兼容时比较 | 本课题首次提出逆向靶点设计 |
| CellOracle、PRESCIENT | 网络扰动、命运方向或带时间的轨迹模拟 | 机制/轨迹参考，非首版必装项 | 用虚拟轨迹替代新组合或时序实验 |

来源：[R3](https://github.com/emdann/pert2state_model)、[R8](https://doi.org/10.1101/2025.06.26.661135)、[R10](https://doi.org/10.1101/2025.04.11.648220)、[R9](https://doi.org/10.64898/2026.01.09.698608)、[R5](https://doi.org/10.1038/s41587-023-01905-6)、[R4](https://doi.org/10.1038/s41551-025-01481-x)、[R6](https://doi.org/10.1038/s41586-022-05688-9)、[R7](https://doi.org/10.1038/s41467-021-23518-w)。STATE、Stack、CellFlow 的能力判断采用本次核实的预印本版本与源码，不将未核实的正式发表状态写成事实。

STATE 主要的欠代表背景实验让训练见到目标背景中 30% 的扰动条件，不能当作完全没有目标扰动数据的证据；其默认独热编码也不能据此获得未见靶基因泛化。Stack 可利用其他背景的响应作提示，但无 Th2 干预真值时不能证明误差被校准。两者均应在本项目重新验证。[R8](https://www.biorxiv.org/content/10.1101/2025.06.26.661135v2.full)、[R9](https://www.biorxiv.org/content/10.64898/2026.01.09.698608v2.full)

研究贡献可以沿三条可检验路线组织：

- **背景迁移的可靠性。** 同一候选在一般 CD4、真实 Th2 和独立供者中是否保持作用方向？有限 Th2 校准能否改善独立测试，而不只是提高训练拟合？
- **给定预算内的少基因达标。** 在固定候选范围、干预类型和阈值下，组合是否比最佳单基因更有效，删掉成员后是否仍达标？
- **预测失败的边界。** 在什么背景或候选上拒绝推荐，拒绝之后准确性和候选覆盖率如何变化？

仅加几个损失项、换一个编码器、拼接现成模型，不足以构成强方法创新。若最终主要发现一个重要调控机制而模型增益有限，应据证据组织为生物学发现工作，而不是勉强包装成通用方法。

## 5. V0：我建议现在实施的版本

V0 的问题是：**在质量可靠、可执行的基因敲低中，哪些扰动的转录效应与预定义的 Th17−Th2 目标方向一致，且这种提名对 guide、数据背景及参考选择较稳定？**

交付是候选证据表，不是已经验证的转换路径。V0分成数据整理、目标定义、单基因排名、稀疏提名与有限验证五步。

### 5.1 先获取小型摘要，不急着补齐全库

作者同时公开了计数矩阵、伪总体和差异表达结果。伪总体（pseudobulk）指按供者、guide、实验条件等合并细胞计数，用于保留实验层级的分析。以下对象大小来自本次 S3 清单，不是估计值：[R2](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/data_sharing_readme.md)、[公开对象清单](https://genome-scale-tcell-perturb-seq.s3.amazonaws.com/?list-type=2&prefix=marson2025_data/)

| 数据 | 大小约值 | 使用顺序 |
|---|---:|---|
| `suppl_tables/sample_metadata.suppl_table.csv` | 3 KB | 首先核对供者/批次/条件 |
| `suppl_tables/sgrna_library_metadata.suppl_table.csv` | 9.94 MB | 校订guide靶基因 |
| `suppl_tables/DE_stats.suppl_table.csv` | 7.28 MB | 质量摘要；**不能替代完整效应矩阵** |
| `GWCD4i.DE_stats.h5ad` | 16.79 GB | V0直接读取完整效应矩阵，减少重复计算 |
| `GWCD4i.pseudobulk_merged.h5ad` | 44.57 GB | 需要按供者重新估计效应时再获取 |
| `GWCD4i.DE_stats.by_donors.h5mu` | 16.87 GB | 可作辅助；供者对有重叠，不能随意划分 |
| `GWCD4i.DE_stats.by_guide.h5mu` | 29.42 GB | guide级验证有需要时再获取 |

全供者 DE 可用于发现候选，但不能随后把其中一个供者当作完全没见过的独立测试。V1要从训练供者重新估计效应、筛特征和调参；不得复用含测试结果的筛选名单。

### 5.2 正确构建“细胞—干预—背景”表

将文件名与作者样本表对应，新增 `donor_id`、`culture_condition`、`run_id`、`dataset_id`。以作者 `sgRNA` 与细胞 `guide_id` 连接，使用校订后的 `target_gene_id` / `target_gene_name`，另保留原始标签与映射状态。不要简单按 guide 名最后一个连字符拆靶基因。[R2](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/data_sharing_readme.md)

当前文件的初步筛选逻辑可表达为：

```python
# 仅示意数据规则；实施时先审计所有类别和缺失值。
qc_ok = ~obs["low_quality"]
single = obs["guide_group"].eq("targeting single sgRNA")
is_control = qc_ok & single & obs["guide_type"].eq("non-targeting")
is_target = (
    qc_ok & single
    & obs["guide_type"].eq("targeting")
    & obs["target_gene_id_curated"].notna()
)
```

这里名称含 `targeting` 的单 guide 分组也包含 NTC，是实测发现的标签特征。`no sgRNA` 不能直接当作 NTC；`multi sgRNA` 在缺乏完整组合身份时排除。优先在同供者、同刺激、同处理批次内匹配 NTC；泳道内对照足够时增加泳道匹配，稀疏时合并需保留记录。

在上述筛选后，再检查 guide 细胞数、靶基因表达、敲低有效性、跨 guide 一致性以及潜在邻近/远端脱靶。建议试行“每个 guide×供者×条件至少20个细胞、每个基因尽量至少2条有效guide”，并对10/20/50细胞门槛做敏感性分析；这些是项目默认值，不是论文统一标准。只有一条可靠guide的候选单独降级标注，不假定其一定无效。

不要用下游表型是否符合预期来排除“敲低失败细胞”，否则可能只保留模型喜欢的响应。有效性判定应独立于目标表型；用于正式预测评估的筛选规则必须从训练数据锁定。

### 5.3 锁定 Th2 起点与 Th17 目标

目前未确认你自己的目标数据。可用公共备选是 Cano-Gamez 等的人源 CD4 极化研究及其公开下载，单细胞数据包含 Th2、Th17 等条件。[R13](https://doi.org/10.1038/s41467-020-15543-y)、[R14](https://www.ebi.ac.uk/biostudies/studies/S-BSST2978)

本次核实 BioStudies 提供约4.46MB的整体 RNA 计数包、8.03GB的单细胞包。**优先用小计数包构建目标差异，待群体模型确有需要再用单细胞包。** 这些包尚未在本次下载并逐样本审计，必须先检查供者、初始/记忆来源、极化条件和时间标签能否准确对应。

默认计算目标采用同研究、相同供者与初始 CD4 来源、同一培养时长的 Th2 与 Th17 极化群体，优先检查该研究5天终点。不要把初始来源 Th2 与记忆来源 Th17 混为一个差异，也不要仅靠一个标志基因给公共扰动细胞贴 Th2/Th17 标签。这两个参考群体不证明同一批 Th2 真的转为了 Th17。

在开始排序前，保存 `target_contract.md`，至少锁定：物种与起点来源；目标亚状态及培养背景；供者与时间；用于优化的基因程序；独立验证的读出；允许的敲低动作；成功阈值如何由独立 NTC 和目标阳性对照确定。不要把 Th17 预设为更健康或治疗性状态。

若采用作者 DE 的 `log_fc` 层，效应单位为 log2 倍数变化。目标也用同一尺度的、供者阻断的差异估计，不能把 log2 倍数变化、自然对数归一化均值差和 z 分数直接相加：

$$
t_j=\operatorname{LFC}^{\log_2}_{j}(\mathrm{Th17}/\mathrm{Th2}),
\qquad
\delta_{g,c,j}=\operatorname{LFC}^{\log_2}_{j}(g\text{-KD}/\mathrm{NTC}\mid c).
\tag{2}
$$

j 是读出基因，g 是干预靶基因，c 是供者与实验条件组成的背景。KD 表示表达敲低，不等于敲除。两类 LFC 在共同基因上对齐；记录跨平台的测量差异。目标基因均值或差异估计可用于 V0，但不能直接当作 V1 所需的完整单细胞分布。

如果从 D1 计数另做探索性效应，不将单供者内的细胞或guide伪装成独立供者给出人群显著性；主要报告效应量、guide一致性与条件性不确定性。若目标样本元数据无法核实，先完成数据准备，暂不交付“Th2→Th17”排名。

### 5.4 先做单基因排名，再做稀疏提名

构造每个条件的效应字典：

$$
D_c=[\delta_{g_1,c},\ldots,\delta_{g_p,c}]
\in\mathbb{R}^{m\times p},
\qquad t\in\mathbb{R}^{m}.
\tag{3}
$$

m 为共同读出基因数，p 为通过质量审核的候选动作数。为防止不可靠基因主导结果，使用固定的非负对角权重 W；先设单位权重作基线，再测试根据训练/参考估计精度截断加权的版本，不能从最终测试结果学习权重。

单基因的相对目标改善可定义为：

$$
R_{g,c}=
\frac{\lVert t\rVert_W^2-\lVert t-\delta_{g,c}\rVert_W^2}
{\lVert t\rVert_W^2+\epsilon},
\qquad
\lVert v\rVert_W^2=v^\top Wv.
\tag{4}
$$

epsilon 是防止分母为零的小正数。该分数既考虑方向，也惩罚幅度不合适；不是转换概率。若目标差异本身接近零或不稳定，应暂停排名。各刺激条件分别排名，并呈现方向冲突；不要强制要求所有时间条件都一样，也不要无依据取三个条件中的最高分。

再复现作者原法，并建立原始动作基上的非负稀疏版本：

$$
\beta^*=\arg\min_{\beta\geq0}
\left\{
\frac{1}{2}\lVert t-D_c\beta\rVert_W^2
+\lambda_1\lVert\beta\rVert_1
+\frac{\lambda_2}{2}\lVert\beta\rVert_2^2
\right\}.
\tag{5}
$$

这里两个正则系数控制稀疏程度与系数稳定性。非零系数表示有助于重建目标的扰动特征，不是实际敲低剂量，也不证明多基因联用会产生该结果。负向敲低特征可能提示反向干预研究价值，但不能自动解释成该基因激活一定有效。

拟实施版本用不旋转扰动轴的、无自由截距的加权回归，并与作者原法分开报告。若对字典列缩放，应保存缩放量并变回原作用尺度。必须审计直接靶基因是否独自驱动得分，增加预先冻结的下游程序与去靶读出敏感性分析。

**作者代码需先做小样本回归检查。** 本次固定提交 `2c2e309...` 的静态核查发现：默认 `l1_ratio` 网格包含大于1的值；非降维 `predict()` 路径与训练标准化路径不一致；降维后正系数约束不保证原靶点系数非负。这些是需验证和适配的源码风险，本次没有执行该模型，也没有修改作者仓库。实施时显式使用有效参数网格，验证训练/预测变换一致，记录原法与修正适配版，不把缺陷造成的劣势当作方法优势。[R3核心代码](https://github.com/emdann/pert2state_model/blob/2c2e30959ffafadecc6af5d4d7b5bde868ab5313/src/pert2state_model/Perturb2StateModel.py)

### 5.5 V0 输出、验证与退出条件

首轮可先从50–100个质量较好的靶基因跑通流程，再扩展到所有通过审核的候选；训练或验证候选的抽样不能只保留预期有利基因。输出一张表，每行至少包含：靶基因与guide、背景、有效细胞数、敲低证据、脱靶标记、单基因得分、稀疏选择频率、guide一致性、供者证据、目标参考敏感性，以及“仅计算提名/有外部证据/待实验”的等级。

V0 至少比较：单基因方向匹配、直接距离改善、作者 pert2state、非负稀疏版本与质量匹配随机排序。若只有一个参考研究，读出基因的分块留出只能检验特征重建，不能伪称独立供者或新实验验证。重采样应保留供者/培养样本/guide层级；仅D1时只报告本供者条件性稳定性。

作者公开的 Th1/Th2 验证面板可作有限的外部方向检验，但候选经过作者挑选，也不是 Th2→Th17 真值。未验证基因不能当负例，不能据该面板估计全基因组无偏命中率；若提前用于调参，便不再是最终独立测试。[R2验证表说明](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/data_sharing_readme.md)

V0完成标准：数据与特征对齐可复查、目标元数据通过审计、基线结果可复现、候选有证据分层，并明确哪些建议只是跨背景外推。是否有高分候选不是软件验收条件；无稳定信号同样应交付，并据原因决定补数据还是停止目标外推。

## 6. V0 完成后：V1 单步响应模型与背景验证

V1应拆成两个不能相互替代的目标：**V1a 检验已见干预向独立供者迁移；V1b 检验公共 CD4 响应能否迁移到真实 Th2。** 只完成V1a不能声称已解决V1b。

### 6.1 训练数据是什么

每个训练样本包含同背景的对照细胞集合、干预标识、背景信息和另一个未配对的扰动细胞集合：

$$
(X_{0,c},g,c)\longmapsto X_{g,c},
\qquad
\widehat X_{g,c}=F_\theta(X_{0,c},g,c).
\tag{6}
$$

这不是逐个细胞一一对应。初版背景使用供者、刺激条件和处理批次；不要用扰动后细胞聚类出的“Th2/Th17”标签冒充干预前状态，从而人为构造状态依赖因果训练对。

保留原始计数用于质量与伪总体分析；供神经模型使用的归一化表达另存，避免误把变换后的值当计数：

$$
\widetilde x_{ij}=\log\!\left(1+10^4\frac{x_{ij}}{L_i}\right),
\qquad L_i=\sum_{j\in\mathcal G_{\rm bio}}x_{ij}.
\tag{7}
$$

这里采用自然对数；生物表达基因集合排除PuroR等技术特征，零总计数细胞应在此前排除。首版固定使用约2000个高变基因加预定功能读出的**非负归一化表达，不做中心化或z分数变换**；高变基因及任何正比例缩放均在训练数据上拟合并冻结。

128维主成分分析（PCA）只保留为后续适配选项，不与首版并列。固定版本STATE在没有内部基因解码器等路径下使用ReLU截断输出，不能直接表示带负值的PCA坐标；外接逆变换不能修复此前的截断。若以后启用PCA或中心化表达，必须先修改并验证有符号输出路径，再检查维度、负坐标与逆变换一致性；不能把潜在坐标解释为真实计数。[STATE输出路径](https://github.com/ArcInstitute/state/blob/9bbfe78a434a55205e4de834e1ea99f85f7a3add/src/state/tx/models/state_transition.py#L513)

### 6.2 先选一个主模型，而不是同时搭建三套大型系统

推荐顺序：无变化/均值/低秩线性基线 → STATE小型ST → 如需进一步比较，再加入CellFlow。Stack仅在其提示数据、基因对齐、许可和资源均满足时作为附加迁移基线，不是必需的“校准层”。

STATE官方小配置采用集合长度128、隐藏维672、4层、8头；本项目可先试集合长度64、隐藏维256、4层等缩小配置，保留官方小配置作为后续比较。实际字段、维度兼容和参数量在固定版本实例化时核实。600M状态嵌入模型（SE）不是本项目第一阶段训练目标。[小ST配置](https://github.com/ArcInstitute/state/blob/9bbfe78a434a55205e4de834e1ea99f85f7a3add/src/state/configs/model/state_sm.yaml)

首个容量试验限定50–100个靶基因、一个已完整背景、约1万–5万细胞；通过后才扩展到100–300个靶基因、10万–30万细胞与多个背景。按guide和背景限制采样，保留完整的独立测试组，不靠重复抽样冒充更多独立数据。默认独热编码只评价已见干预迁移；未见基因任务另需外部基因表示与完整留靶基因划分。

### 6.3 损失函数保持简单，并避免重复计分

主损失可采用最大均值差异（MMD）来比较两组样本的分布。一个有偏、非负的经验估计写为：

$$
\begin{aligned}
\widehat{\mathrm{MMD}}^{2}(U,V)
={}&\frac{1}{n^2}\sum_{i,i'}k(u_i,u_{i'})
+\frac{1}{m^2}\sum_{j,j'}k(v_j,v_{j'})\\
&-\frac{2}{nm}\sum_{i,j}k(u_i,v_j).
\end{aligned}
\tag{8}
$$

核函数k使用训练/验证集合上固定的多带宽高斯核，带宽不从最终测试响应选择。它比较分布，不恢复真实的细胞配对。计算在统一缩放的模型表达空间进行。

式（8）–（9）是本项目拟比较的适配方案，不是宣称官方配置已经实现全部损失。先保留固定版本STATE的原生损失跑通基线，再实现并比较以下分布损失、加权均值匹配和NTC分布一致性三项，分别记录版本与消融结果：

$$
\begin{aligned}
\mathcal L={}&\widehat{\mathrm{MMD}}^{2}(\widehat X_{g,c},X_{g,c})
+\lambda_\mu\lVert\widehat\mu_{g,c}-\mu_{g,c}\rVert_W^2\\
&+\lambda_0\widehat{\mathrm{MMD}}^{2}
\bigl(F_\theta(X_{0,c},\mathrm{NTC},c),X'_{0,c}\bigr).
\end{aligned}
\tag{9}
$$

其中X′为同背景独立采样的NTC集合，均值和权重 W 均定义在本阶段冻结的模型表达空间；不直接沿用 V0 的 LFC 权重数值。先比较只用分布损失，再逐项加入辅助项；不预设它们必然改善性能。若所谓“扰动差值损失”两侧都减去同一个对照均值，它在代数上就是均值匹配：

$$
(\widehat\mu_{g,c}-\mu_{0,c})-(\mu_{g,c}-\mu_{0,c})
=\widehat\mu_{g,c}-\mu_{g,c}.
\tag{10}
$$

所以不能把这类写法包装成新增的因果约束；它的可能价值来自读出权重和训练平衡，需要消融验证。

### 6.4 划分规则比模型名称更重要

| 检验 | 允许的输入 | 独立测试 | 能支持的结论 |
|---|---|---|---|
| D1技术试运行 | D1已测背景 | 泳道/guide分组检查 | 数据和实现可用；不是跨供者 |
| 已见基因、未见供者 | 其他供者的该基因响应 | 完整留出供者 | 已测条件内的供者迁移 |
| 未见条件 | 其他刺激背景 | 完整留出条件，另审批次 | 刺激背景迁移；不是Th2转换 |
| 未见靶基因 | 训练基因及合法外部表示 | 测试基因全部guide一起留出 | 未见基因预测；不能用其真实效应查表 |
| Th2零样本迁移 | 公共CD4响应、未扰动Th2参考 | 独立Th2干预 | 只有拿到真值后才能评价 |
| Th2少样本迁移 | 一部分Th2校准干预 | 未参与校准的干预及新供者 | 少样本迁移，不再称完全零样本 |

当只有D1/D2完整时，可做一个方向的开发—外部供者测试，超参数只在开发数据内选；不能声称稳定的人群泛化。获得更多供者时，按供者作外层留出，训练供者内部调参；报告每位供者而非只汇总一个分数。四位供者仍然不多，置信区间不应被当成充分的人群保证。

同一供者的细胞、guide、重复抽样和训练随机种子均不构成独立供者。训练/测试不能共享供者对统计量中的成员。作者全供者DE、敲低效率和筛选结果若含测试数据，只能用于事后审计，不能用来挑选“容易成功”的测试集。预训练模型使用过什么数据也应记录；重叠不能排除时，不能声称从未见过该数据源。

默认允许留出背景的未扰动NTC作为推理输入，对所有方法采用同一规则；这不等于允许读取其扰动响应来调参、筛特征或校准。供者、条件和批次的匹配必须落实到加载器，原始默认对照名不能照搬。未见供者的类别编码需显式定义未知类别处理，不能假设其已有训练好的供者嵌入。

## 7. 怎样证明模型和候选真的有价值

### 7.1 正向预测评价

至少报告每个干预的效应幅度误差、方向、分布距离、检索/排序表现，并与训练扰动均值及低秩线性模型比较。不能只报全表达相关系数。已有独立研究表明简单模型可能很强，且平均效应可掩盖扰动特异性；这不是断言深度方法永远无用。[R11](https://doi.org/10.1038/s41592-025-02772-6)、[R12](https://doi.org/10.1038/s41587-025-02777-8)

V1的主效应指标固定在式（7）的自然对数归一化表达空间：先对预测与真实细胞使用相同处理，再求群体均值并减去同一匹配NTC均值。下式的上标ℓ表示这一空间，区别于V0中表示log2倍数变化的δ；其中预测均值来自模型输出，真实及NTC均值来自对应观测集合：

$$
\begin{aligned}
\Delta^{(\ell)}_{g,c,j}&=\mu^{(\ell)}_{g,c,j}-\mu^{(\ell)}_{0,c,j},\\
\widehat\Delta^{(\ell)}_{g,c,j}&=\widehat\mu^{(\ell)}_{g,c,j}-\mu^{(\ell)}_{0,c,j},\\
\mathrm{RMSE}_{\Delta^{(\ell)}}(g,c)&=
\sqrt{\frac{1}{m}\sum_{j=1}^{m}
(\widehat\Delta^{(\ell)}_{g,c,j}-\Delta^{(\ell)}_{g,c,j})^2}.
\end{aligned}
\tag{11}
$$

作者发布的log2 LFC另作描述性比较，不与上述均值差直接混算。若以后增加log2 LFC预测指标，需为真实与预测侧共同冻结逆变换、群体聚合、伪计数和效应估计器；自然对数表达均值差除以log(2)，一般不等于群体均值比的log2。实施时先用人工小矩阵检查计算顺序、NTC零效应和变换一致性。

还应相对于**训练扰动中心**检查特异效应，而不是用测试扰动重新定义中心。原始表达、相对NTC的差值、去平均效应后的结果分别呈现。留出基因上的真实DE可作最终描述性评估，但不能用于训练、调参或选择排名目标。

建议的复杂模型升级门槛是：预先选择的主要效应指标相对最佳简单基线有实用幅度改善，例如10%，且没有以破坏控制分布、功能读出或候选覆盖率为代价。10%只是立项默认值，应在最终测试前根据先导噪声和用途锁定；没有足够独立供者时不作“统计证明优越”的表述。

### 7.2 候选设计评价

群体目标不能只剩一个均值。得到真实单细胞目标后，可同时评价全局分布与冻结的功能程序：

$$
D(P,Q)=\alpha D_{\rm global}(P,Q)
+(1-\alpha)\lVert\mathbb E_P\phi(z)-\mathbb E_Q\phi(z)\rVert_2^2,
\quad 0\leq\alpha\leq1.
\tag{12}
$$

P是预测或实测响应群体，Q是目标群体，phi为预先确定的程序评分。全局项可用切片Wasserstein距离或MMD；各项先按开发集尺度归一化，alpha仅在开发数据上锁定。程序至少区分Th17获得、Th2保留/降低及一般激活/应激，不能以整体接近掩盖关键程序缺失。

冻结一组未用于优化的验证读出；单看用来训练分类器的同一组marker变好，会形成循环论证。候选检验以等候选集合、等实验预算进行。没有完整候选真值时，只报告已测面板上的表现，不报告全局召回率或相对未知真实最优解的差距。

如果采用不确定性过滤，应报告“保留多少候选—这些候选表现如何”的曲线。多随机种子离散度、guide重采样、模型分歧和分布外距离只是不同风险信号；没有目标环境校准时，不将它们合成为宣称有覆盖保证的成功概率。

### 7.3 生物学验证的最低设计

先验证少量单基因，例如6–10个：同时包含稳定高排名、不同模型意见分歧、已知参照和质量匹配随机候选。每个靶基因至少考虑2条独立guide，探索阶段以不少于3位独立供者为起点；最终样本量根据先导效应、供者间变异和预设最小实用效应重新设计。3位供者只是探索起点，不是功效充分的证明。

起始细胞需经过明确Th2身份审核，所有组使用匹配培养、时点和NTC。共同测量：目标基因扰动程度；不参与模型优化的Th17蛋白/细胞因子功能；残余Th2程序；活性、增殖及**绝对活细胞数**。RORC/RORγt、IL17A/IL17F与Th2相关读出可列为候选面板，最终组合取决于目标亚状态和实验平台，不能以一个marker的增加宣称完整转换。

只有功能获得仍不够称为命运转换。更强结论需要起点身份、来源/谱系证据或等效的排混设计，排除少量Th17污染细胞选择性扩增，并在预设观察窗口中检查持久性，必要时比较去刺激/再次刺激后的状态。技术重复用于估计测量误差，统计推断以供者和独立培养重复为层级；多候选检验需控制多重比较，报告效应量而不只p值。

## 8. V2：推荐的最终主版本——经实验验证的少基因设计

V1证明部分单基因预测有用之后，再进入组合。第一批可从6个单基因候选出发，形成15个无序双基因组合；保留对应单基因与NTC，候选范围小则直接穷举，没必要先引入束搜索或强化学习。guide组合应有独立实现以检查脱靶与构建效应，不把一个构建代表所有组合证据。

单、双扰动必须匹配递送负荷：在同一体系比较A+B、A+NTC、NTC+B和NTC+NTC，或使用等效的等负荷设计，匹配载体、筛选及培养条件，并测量组合中两个靶点各自的敲低程度。按供者与培养批次设置区组，在区组内随机分配处理。若负荷未匹配，只能报告该构建的组合表型，不能把差异全部归为遗传互作。

V2另存开发与确认清单：组合校准、反馈选优均只用开发数据；在最终确认前冻结候选、比较模型、拒绝规则、主要终点及达标率分母，随后在未参与选优的新供者中确认。若主张未见组合预测，还须额外留出完整组合身份。各方法在共同供者区组内等预算比较，不能把“15个组合×guide×细胞数”当独立供者数；功效设计针对预设主要比较。没有独立确认预算时，明确限定为探索性面板，不报告已确证的方法增益。

同时干预的模型应直接接收集合：

$$
\widehat P_{A,c}=G_\theta(P_{0,c},A,c),
\qquad A=\{g_1,\ldots,g_k\}.
\tag{13}
$$

对同一个集合，输入排列不应改变预测。单扰动训练阶段可用加性估计产生待测假设；只有获得组合真值后，才检验非线性模型是否优于加性、匹配均值和最佳单基因。组合身份已在训练中出现与组合成员单独见过但组合未见，必须分别报告。[R5](https://doi.org/10.1038/s41587-023-01905-6)、[R12](https://doi.org/10.1038/s41587-025-02777-8)

在固定效应尺度上，组合的非加性残差为：

$$
\Gamma_{A,B,c}=\delta_{\{A,B\},c}-\delta_{A,c}-\delta_{B,c}.
\tag{14}
$$

必须同时传播各项不确定性，包括共享NTC、同供者及共同guide构建带来的协方差。非零残差不自动等于功能协同，更不代表治疗获益；“协同”需要明确表型、尺度和参照模型。比最佳单基因好与偏离加性是两个不同检验。

最终优化可写为：

$$
\begin{aligned}
A^*&=\arg\min_{A\subseteq\mathcal G_{\rm allowed}}|A|,\\
\text{subject to}\quad
D(\widehat P_{A,c},P_T)&\leq\varepsilon_T,\\
V(A,c)&\geq v_{\min},\\
U(A,c)&\leq u_{\max},\qquad |A|\leq K.
\end{aligned}
\tag{15}
$$

允许集合固定了可操作的基因与干预方式；K为实验预算，V为经测量/验证的活性或绝对活细胞产量指标，U为经过明确校准或仅作经验筛选的风险指标。阈值来自开发实验与用途，在最终测试前锁定。没有V、U对应真值时，只能报告带缺失约束的候选，不能冒称安全可靠。

与其硬输出一个“最优答案”，应提供效果—干预数—风险的权衡表，并区分三种说法：稀疏解只表示模型选了少量特征；删掉任一成员都不达标支持“不可再删减”；只有在预先限定的候选宇宙中排除更小集合，才能支持该范围内的最少。它们都不等于全生物学空间的全局最优。

最终主版本应具备：冻结的目标契约、可追溯动作库、经独立供者和真实Th2验证的响应模型、组合校准、可拒绝的候选选择，以及实验反馈后的再次比较。研究主结果应是独立达标率、预算利用和可靠性是否提高，而不仅是一张更漂亮的降维图。

如果本项目以后与E3连接酶主线结合，可增加一个人工核验的E3及相关复合体动作子集，与全基因候选同预算比较，并明确区分酶、适配体及相关因子。当前不因工作目录名称而把候选强行限制为E3；RNA扰动特征也不能单独证明蛋白降解或底物机制，相关机制主张需要独立蛋白层证据。

## 9. V3：如果最终要研究干预顺序，还必须补什么

真实时序策略需显式表示仍在生效的既往干预、加入时间、持续时间与培养背景。即使当前表达相同，历史不同也可能产生不同响应，因此不能只把表达分布当作充分状态。

$$
\begin{aligned}
s_t&=(P_t,A_t,h_t,c),\\
A_{t+1}&=A_t\cup\{a_t\},\\
P_{t+1}&=T_\theta(P_t,A_t,a_t,\Delta t_t,h_t,c).
\end{aligned}
\tag{16}
$$

A_t是持续生效的干预集合，h_t记录干预历史与持续性，delta t为真实实验间隔。联合集合更新只适用于持续性加入的动作；若可逆、洗脱或关闭，需另行定义状态更新，不能沿用此简化。

对应证据至少包括A→B、B→A、同时A+B及单干预/时点匹配对照，并测量中间状态。总培养时间、各干预暴露时长和检测时点应合理匹配或显式建模，否则“顺序优势”可能只是暴露时长不同。CellFlow或PRESCIENT对带时间数据的建模提供参考，但不能替代本系统的顺序实验。[R10](https://doi.org/10.1101/2025.04.11.648220)、[R7](https://doi.org/10.1038/s41467-021-23518-w)

只有相应数据和误差检验到位后才用束搜索或模型预测控制；搜索深度是干预次数，不能拿流模型数值积分步数代替。强化学习不是必需组件，尤其不应在未校验的模拟环境里靠高奖励宣称获得真实策略。

## 10. 资源安排：当前32GB显存可以做哪些部分

**足以开展V0和合理缩小的V1容量试验，但本次尚未实测模型训练，不能保证所有官方配置直接运行。** V0主要受CPU内存、磁盘和I/O约束。V2/V3的数据质量与实验成本也不会由增加GPU自动解决。

| 工作 | 当前建议 | 限制与升级条件 |
|---|---|---|
| 元数据、质量审核、V0线性方法 | CPU；优先摘要与分块伪总体 | 不加载整个原始CSR矩阵 |
| V1小ST | 单卡先试；集合64、约2000个非负表达输入 | 混合精度、梯度累积；记录峰值内存/显存；PCA须另适配输出头 |
| V1扩展 | 容量试验通过后增加细胞、背景、模型 | 先看基准增益，再加规模 |
| 第二张32GB卡 | 优先并行不同折、种子或候选模型 | 常规数据并行不会自动把两张卡变成单个64GB显存池 |
| 单张5090 | 按实际可见显存、驱动与框架支持测试 | 不凭型号承诺速度；先检查GPU架构与算子兼容 |
| SE/Stack大规模预训练 | 不纳入本项目 | 需要另行资源和研究目标论证 |

内存量级可由元素数直接估算。例如20万细胞、2000基因、float32输入仅数值数组为：

$$
200{,}000\times2{,}000\times4
=1.6\times10^9\ \mathrm{bytes}\approx1.49\ \mathrm{GiB}.
\tag{17}
$$

这不含复制、稀疏索引、元数据、优化器与模型激活。任何单个百GB原始CSR文件都不适合在62GiB限额下整体读取。先以1024–4096行的小块读取、筛选和聚合，再逐步调节；每次只处理一个源文件，统计时用合适精度避免累计溢出。采样应采用顺序扫描或分块索引，避免海量离散随机磁盘读取。

还要注意：`AnnData` 的 `backed="r"` 并不自动保证所有附属层都延迟加载。作者 DE 文件的多个 `.layers` 可能占较大内存；V0只需部分统计量时，优先用HDF5接口按层、按块读取并核对标签，不一次加载全部层。衍生文件只保存所需基因、抽样细胞和明确的计数/归一化层。

建议工作预算：进程峰值CPU内存尽量控制在45GiB以内，至少预留100–150GB用于新增摘要、参考数据、分片和模型输出；实际额度随选择的输入调整。完整恢复D2_Stim8hr还需约60GB额外文件空间，不包括备份；不自动删除已有文件为其让位。

STATE、Stack与CellFlow应各自固定代码版本并核实代码/权重许可；本次发现部分包元数据与仓库许可声明不一致，详见模型核查附件。不要把“公开代码”自动解释成所有用途均获许可。

## 11. 接下来按什么顺序实施

以下时间是工作量估计，不是训练速度或实验成功保证；假定有一名能持续推进的计算研究者，实验阶段另有相应平台。

| 工作包 | 预计范围 | 明确交付物 | 进入下一步的条件 |
|---|---|---|---|
| P0：数据清单与目标契约 | 1–3个工作日 | 文件清单、校订映射、参考样本表、目标契约 | 源文件可读；样本条件可核对 |
| P1：V0小规模复现 | 约1周 | 50–100靶基因试运行、三类基线、尺度与代码检查 | 无基因错位/泄漏/负动作误解 |
| P2：V0候选交付 | 再1–2周 | 质量过滤后的候选表、稳定性、失败/拒绝说明 | 可解释的候选证据，不要求必须发现有效基因 |
| P3：V1a独立背景预测 | 约2–4周 | 冻结划分、简单基线、小ST基准 | 有完整独立供者，复杂模型有实际增益才扩展 |
| P4：V1b真实Th2验证 | 按平台估计约6–10周 | 校准/独立验证分离的单基因效应与功能结果 | 排除一般激活、污染与选择性存活等解释 |
| P5：V2组合闭环 | 单基因结果之后约2–4个月 | 单/双干预面板、组合模型、删减验证与持续性 | 有可复现达标组合；无则诚实报告范围内未找到 |
| P6：V3顺序扩展 | 不预设期限 | 时序面板与独立策略测试 | 有真实顺序数据及进一步研究价值 |

后续工程目录可以按以下职责建立；这是实施结构，不代表本次已经生成训练流水线：

```text
configs/          数据、目标、模型和冻结划分
metadata/         文件清单、样本表、guide映射、质量审计
processed/        小型伪总体、效应字典、抽样单细胞分片
src/data/         只读分块处理与对齐
src/baselines/    均值、线性、pert2state及非负版本
src/models/       STATE/CellFlow适配层
src/design/       单基因排名、集合枚举、拒绝规则
results/v0/       候选、证据、敏感性和目标重建
results/v1/       独立划分、预测和校准报告
results/v2/       实验面板、组合与最小性证据
```

第一个应启动的任务是P0/P1：固定数据清单、校订guide、读取目标参考样本表，建立约50–100基因的V0试验。不要把“先训练STATE”设为启动项目的前提。恢复D2是V1的准备工作，不阻止基于已完整D1和公开汇总的V0准备。

## 12. 必须允许失败，并把失败转成下一步决策

| 观察到的结果 | 应采取的行动 |
|---|---|
| 参考Th2/Th17样本条件无法核对 | 先补元数据或更换明确参考，暂停目标排名 |
| 目标主要由一般激活/应激区分 | 重定目标和独立读出，不能继续用高分类分数自证 |
| 两条guide方向冲突、疑似脱靶 | 降级候选，补guide或实验，不直接平均掉冲突 |
| 只有D1 | 仅称单供者结果，暂不进行跨供者验收 |
| 复杂模型不优于强基线 | 保留基线，优先做实验，不继续无条件加参数 |
| CD4预测能复现，但Th2验证失败 | 说明背景迁移失败，补Th2校准数据或限制适用背景 |
| 双基因没有优于单基因 | 采用更少干预，不能为了“组合创新”硬保留双基因 |
| 群体更像目标但绝对活细胞大幅减少 | 优先检查选择性存活，不称重编程成功 |
| 所有已测候选都不达标 | 报告在该动作集、培养背景和预算内未找到方案；不能推断生物学不可能 |

## 13. 最终版应该是什么，以及不必是什么

推荐最终主版本是**有明确适用背景、能够拒绝不可靠推荐、经独立供者与真实Th2组合实验验证的少基因干预设计方法**。它至少交付冻结的目标、可复查候选与对照、经验证的单步/组合预测、效果与干预数量的权衡，以及足以区分表达偏移、功能获得和持续转换的证据。

若顺序确有额外价值，再增加V3；不要求第一篇工作同时完成大型基础模型、强化学习、通用动力学、蛋白机制和所有组合。最终结果也可能是某些目标在当前动作范围内无法可靠到达，或线性方法已足够有效，这些都是需要保留的科研结论。

本次已经完成的是方案调研、数据抽样核查与文档排版核验；没有执行候选筛选、训练、实验或获得转换结果。

## 14. 主要原始来源与随附核查材料

1. **R1** Zhu、Dann等，*Genome-scale perturb-seq in primary human CD4+ T cells maps context-specific regulators of T cell programs and human immune traits*，Cell，2026-08-28在线。[论文](https://doi.org/10.1016/j.cell.2026.08.002)
2. **R2** 作者数据字段、处理与验证资料说明。[数据说明](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/data_sharing_readme.md)
3. **R3** `pert2state_model`作者实现，本次核查固定提交`2c2e309...`。[代码](https://github.com/emdann/pert2state_model/blob/2c2e30959ffafadecc6af5d4d7b5bde868ab5313/src/pert2state_model/Perturb2StateModel.py)
4. **R4** Gonzalez等，*Combinatorial prediction of therapeutic perturbations using causally inspired neural networks*，Nature Biomedical Engineering，2025在线。[PDGrapher](https://doi.org/10.1038/s41551-025-01481-x)
5. **R5** Roohani等，*Predicting transcriptional outcomes of novel multigene perturbations with GEARS*，Nature Biotechnology，2023在线。[论文](https://doi.org/10.1038/s41587-023-01905-6)
6. **R6** Kamimoto等，*Dissecting cell identity via network inference and in silico gene perturbation*，Nature，2023。[CellOracle](https://doi.org/10.1038/s41586-022-05688-9)
7. **R7** Yeo等，*Generative modeling of single-cell time series with PRESCIENT enables prediction of cell trajectories with interventions*，Nature Communications，2021。[论文](https://doi.org/10.1038/s41467-021-23518-w)
8. **R8** *Predicting cellular responses to perturbation across diverse contexts with State*，bioRxiv，本次核查v2。[论文](https://doi.org/10.1101/2025.06.26.661135)、[代码](https://github.com/ArcInstitute/state)
9. **R9** *Stack: In-Context Learning of Single-Cell Biology*，bioRxiv，本次核查v2。[论文](https://doi.org/10.64898/2026.01.09.698608)、[代码](https://github.com/ArcInstitute/stack)
10. **R10** *CellFlow enables generative single-cell phenotype modeling with flow matching*，bioRxiv，本次核查v1。[论文](https://doi.org/10.1101/2025.04.11.648220)、[代码](https://github.com/theislab/CellFlow)
11. **R11** Ahlmann-Eltze等，*Deep-learning-based gene perturbation effect prediction does not yet outperform simple linear baselines*，Nature Methods，2025。[论文](https://doi.org/10.1038/s41592-025-02772-6)
12. **R12** *Systema: a framework for evaluating genetic perturbation response prediction beyond systematic variation*，Nature Biotechnology，2025在线。[论文](https://doi.org/10.1038/s41587-025-02777-8)
13. **R13** Cano-Gamez等，*Single-cell transcriptomics identifies an effectorness gradient shaping the response of CD4+ T cells to cytokines*，Nature Communications，2020。[论文](https://doi.org/10.1038/s41467-020-15543-y)
14. **R14** Open Targets极化参考的公开归档。[项目入口](https://opentargets.org/projects/effectorness)、[BioStudies S-BSST2978](https://www.ebi.ac.uk/biostudies/studies/S-BSST2978)
15. **R15** STATE当前依赖与小模型配置。[依赖](https://github.com/ArcInstitute/state/blob/9bbfe78a434a55205e4de834e1ea99f85f7a3add/pyproject.toml)、[配置](https://github.com/ArcInstitute/state/blob/9bbfe78a434a55205e4de834e1ea99f85f7a3add/src/state/configs/model/state_sm.yaml)
16. **R16** CellFlow依赖与数据接口。[依赖](https://github.com/theislab/CellFlow/blob/446ed6073c60ac2e8db13c4ea096a43cdec288b2/pyproject.toml)、[接口](https://github.com/theislab/CellFlow/blob/446ed6073c60ac2e8db13c4ea096a43cdec288b2/src/cellflow/model/_cellflow.py)
17. **R17** Qin等，*Cell fate conversion prediction by group sparse optimization method utilizing single-cell and bulk OMICs data*，Briefings in Bioinformatics，2021。[论文](https://doi.org/10.1093/bib/bbab311)
18. **R18** CD4原始实验供者、条件与批次表。[样本表](https://github.com/emdann/GWT_perturbseq_analysis_2025/blob/master/metadata/suppl_tables/sample_metadata.suppl_table.csv)

本地附件位于 `research/`：`data_and_target_evidence.md` 为数据与参考核查，`model_evidence.md` 为模型版本/接口/资源核查，`novelty_evidence.md` 为近邻文献与可识别性分析，`server_audit.json` 为实测记录。`perturb_implementation_plan.html` 为同源生成、字体内嵌的离线公式预览；截图与自动检查结果位于 `research/qa_preview/`。公式渲染通过只能证明显示语法与布局可用，不能替代方法和实验的科学验证。
