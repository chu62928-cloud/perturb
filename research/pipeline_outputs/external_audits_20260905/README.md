# D2_Stim8hr 与 D4_Stim48hr 新文件审计

审计日期：2026-09-05。原始 `.h5ad` 文件只读打开，按 1,024 个细胞逐块读取 `X/indptr`、`X/indices`、`X/data`，并核对 `X` 与 `obs/var` 形状、指针边界和单调性、索引范围、表达值有限且非负，以及审计前后文件大小、修改时间和采样指纹。

## 结论

| 文件 | 全量 CSR 结果 | 关键结果 |
|---|---|---|
| D2_Rest | PASS | 源文件未变化，复用 2,872 个已验证块 |
| D2_Stim8hr（新上传） | PASS | 3,032,848 行对应 2,962 个块全部重新读取；`indices=data=14,352,017,864` |
| D2_Stim48hr | PASS | 源文件未变化，复用 2,797 个已验证块 |
| D4_Stim48hr（新上传） | BLOCKED_D2_CSR | 第 2,021 块发现 1,537 个越界索引 |

D2 三个条件的汇总为 `status=PASS`、`all_full_csr_read_valid=true`。新 D2_Stim8hr 已越过旧文件曾经失败的 B-tree 区间，且本次完整读取至文件末尾，没有重现旧错误。

D4_Stim48hr 的失败块为行 `2,069,504:2,070,528`，对应非零范围 `9,774,408,325:9,779,444,902`。独立只读探针确认矩阵形状为 `2,815,784 × 18,130`，该块包含 1,537 个越界索引；首个异常绝对位置为 `9,778,743,625`，值为 `4,575,657,221,408,425,920`，远超合法基因索引 `0–18,129`。同一块的表达值仍为有限、非负的 `float32`，因此问题定位为 `X/indices` 内容损坏，而不是简单的文件大小或登记时间不一致。源文件在审计前后保持一致。

元数据盘点显示预期 12 份 D1–D4 文件中当前存在 11 份；仍缺少 `D3_Rest.assigned_guide.h5ad`。本次为审计新文件生成了独立登记快照，没有覆盖历史注册表。D2 虽已满足完整 CSR 审计条件，但本次没有自动激活 D2 角色、读取生物学响应或生成效应矩阵；D4_Stim48hr 在修复或重新下载前不得用于外部测试。
